# This service provides a centralized logging instance used throughout the LangGraph workflow to track agent execution, decisions, and debugging information.

import logging

logger = logging.getLogger("marketmind")